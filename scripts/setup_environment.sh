### Create a virtual environment, activate it and install the requirements

# Run using `source ./scripts/setup_environment.sh`

python3 -m venv .venv

source .venv/bin/activate

pip install -U pip
pip install -r requirements.txt