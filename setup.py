import setuptools

setuptools.setup(
    name='pipeline_mvp',
    packages=setuptools.find_packages('pipeline_mvp'),
    package_dir={'': 'pipeline_mvp'},
    setup_requires=[],
    tests_require=[],
    install_requires=[],
)
