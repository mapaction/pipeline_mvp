resources:
  cmf:
    config:
      location: /home/turnerm/data/yemen_cmf
      event_id: 2020yem01
storage:
  filesystem:
    config:
      base_dir: /tmp
loggers:
  custom_console_logger:
    config:
      log_level: INFO

